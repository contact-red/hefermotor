# syntax-field-after-method

A field after a method. Both report at the `var`: ponyc's restart check after its `members` sequence, and hefermotor's member list, which reports a method expected there and keeps the field.

ponyc: `main.pony:3:3: syntax error: unexpected token var after type, interface, trait, primitive, class or actor definition`.
