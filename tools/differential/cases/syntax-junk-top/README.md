# syntax-junk-top

A `]` between two entities. Both report at the `]`: ponyc's `class_def` restart check, hefermotor's module rule.

ponyc: `main.pony:2:1: syntax error: unexpected token ] after type, interface, trait, primitive, class or actor definition`.
