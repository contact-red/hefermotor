# syntax-use-mid-entity

A `use` between two members. Both report at the `use`; hefermotor takes the command as the member list's error item and keeps both methods.

ponyc: `main.pony:3:3: syntax error: unexpected token use after type, interface, trait, primitive, class or actor definition`.
