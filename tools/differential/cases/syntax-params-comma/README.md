# syntax-params-comma

A parameter list holding only a comma. Both report at the `,`: the parameter list is entered only on a name, so the `)` is expected there.

ponyc: `main.pony:2:9: syntax error: expected ) after (`.
