# sample-parse-error

A method body cut inside a parenthesised expression. Both reject at `--pass=parse`, and every position ponyc reports is one hefermotor reports, so the case counts into `sample positions` and `sample first position` on the summary line; a `sample-` case has no `EXPECT`, so the count of positions is not checked.

ponyc: `main.pony:2:21: syntax error: expected value after +`.
