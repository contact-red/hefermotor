# syntax-missing-end

An `if` left open at the end of the file. Both report at the `if`.

ponyc: `main.pony:3:5: syntax error: unterminated if expression`, with `3:18: expected terminating end before here`.
