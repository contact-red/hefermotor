"""
# hefermotor/source

Names and hashes a check's inputs: a source file with its content hash, a
package's display name, the build configuration, and the content hash
that is the only identity allowed to cross a cache boundary.
"""
